[project]
name = "pystormtracker-data"
version = "0.0.0"
requires-python = ">=3.14"
dependencies = [
    "cdsapi>=0.7.7",
    "netCDF4>=1.7",
    "numpy>=2.0",
    "xarray>=2025.1",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

[tool.uv]
package = false

# PyStormTracker-Data Instructions

- Use Python >=3.14 with `uv sync --locked` for environments and dependencies.
- Run tests with `uv run pytest`.
- Add or update focused tests for behavioral changes.
- Use direct technical prose and avoid unsupported claims.
- Keep changes limited to the requested task.
- Do not stage, commit, push, publish, or create releases unless explicitly requested.
